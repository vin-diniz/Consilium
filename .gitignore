
# Documentação interna SPIDERRANK (não commitar)
benchmark-advocacia-empresarial.md
contexto-cidades-pr.md
_commit_msg.txt
*.backup-pre-sprint-*
_sprint*_t*.py

# IDE / Editor (config local, não compartilhado)
*.code-workspace
.vscode/
.idea/

# Fontes brutas de imagem (mantidas locais como fonte para regerar variantes)
# As variantes -480/-960 .avif/.webp/.jpg sao commitadas e servidas pelo CDN
assets/grothperfil.jpg

# Node aux (sharp instalado pontualmente para gerar variantes)
node_modules/
package-lock.json
package.json
